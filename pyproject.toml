[project]
name = "nucl-parquet"
version = "0.1.0"
description = "Nuclear data (cross-sections, stopping powers, decay) as Parquet files"
requires-python = ">=3.11"

[dependency-groups]
dev = [
    "polars>=1.0",
    "pytest>=8.0",
]
