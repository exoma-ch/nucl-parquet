{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "nucl-parquet catalog",
  "type": "object",
  "required": [
    "version",
    "data_version",
    "data_sha256",
    "libraries",
    "shared"
  ],
  "properties": {
    "version": {
      "type": "integer",
      "const": 1
    },
    "data_version": {
      "type": "string",
      "pattern": "^[0-9]{4}\\.[0-9]+\\.[0-9]+$",
      "description": "CalVer YYYY.MM.MICRO identifier for the data tarball (e.g. `2026.5.0`, `2026.5.1` for an in-month iteration); matches the GitHub Release tag `data-{data_version}`."
    },
    "data_sha256": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$",
      "description": "Deterministic SHA-256 tree hash of all data/**/*.parquet files. Recompute via `nucl_parquet.compute_data_sha256()`. PR CI gates on hash matching the on-disk tree; data_version must change iff data_sha256 changes."
    },
    "description": {
      "type": "string"
    },
    "libraries": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "description": "Two shapes supported: (1) evaluated cross-section libraries with `projectiles`, `version`, `path`; (2) external-dataset references (e.g. strata-data on HF) with `revision`, `fetcher`, `files`. The common required minimum is `name` + `data_type`.",
        "required": [
          "name",
          "data_type"
        ],
        "properties": {
          "name": {
            "type": "string"
          },
          "description": {
            "type": "string"
          },
          "source_url": {
            "type": "string",
            "format": "uri"
          },
          "projectiles": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "Light-ion shorthand (n/p/d/t/h/a/g) for evaluated libraries; heavy-ion identifiers (e.g. c12, fe56) for production / total-reaction libraries."
          },
          "data_type": {
            "type": "string",
            "enum": [
              "cross_sections",
              "transport_cross_sections",
              "production_cross_sections",
              "total_reaction_cross_sections",
              "experimental_cross_sections",
              "nuclear_structure",
              "nuclear_structure_and_em",
              "regulatory_limits",
              "dose_coefficients"
            ]
          },
          "version": {
            "type": "string"
          },
          "builder": {
            "type": [
              "string",
              "null"
            ],
            "description": "Repo-relative path of the script that produced this library's parquets, or `null` if it was produced by a pipeline that is not in this repository (see #346). Required for every library that declares a `path`. Its digest is stamped into the library's manifest.json so a builder fix that never reached the data is detectable — see nucl_parquet/builder_stamp.py and #342."
          },
          "rebuild_command": {
            "type": "string",
            "description": "The literal command that re-ingests this library, rendered in the staleness failure message. `<scratch>` is a throwaway output directory; copy the result over `data/<library>/` afterwards."
          },
          "path": {
            "type": "string"
          }
        }
      }
    },
    "shared": {
      "type": "object",
      "properties": {
        "meta": {
          "type": "object",
          "required": [
            "path",
            "files"
          ],
          "properties": {
            "path": {
              "type": "string"
            },
            "files": {
              "type": "object",
              "additionalProperties": {
                "type": "string"
              }
            }
          }
        },
        "stopping": {
          "type": "object",
          "required": [
            "path",
            "sources"
          ],
          "properties": {
            "path": {
              "type": "string"
            },
            "layout": {
              "type": "string"
            },
            "sources": {
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "sources_note": {
              "type": "string"
            },
            "catima": {
              "type": "object"
            }
          }
        }
      }
    }
  }
}
